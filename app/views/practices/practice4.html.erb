<script type="text/javascript">
  var h = 200;
  var w = 500;
  var barPadding = 1;

  var dataSet = [ 5, 10, 13, 19, 21, 25, 22, 18, 15, 13,
                  11, 12, 15, 20, 18,19, 21, 25, 22, 18, 15, 13] ;

  var svg = d3.select("body")
              .append("svg")
              .attr("width", w)
              .attr("height", h)

    svg.selectAll("rect")
     .data(dataSet)
     .enter()
     .append("rect")
     .attr({
          x: function(d, i) { return i * (w / dataSet.length); },
          y: function(d) { return h - (d * 4); },
          width: w / dataSet.length - barPadding,
          height: function(d) { return d * 4; },
          fill: function(d) { return "rgb(0, 0, " + (d * 10) + ")"; }
     })

     //Text have to be added from the svg using selectAll
     svg.selectAll("text")
      .data(dataSet)
      .enter()
      .append('text')
      .text(function (d) { return d })
      .attr("x", function (d, i) { return i * (w / dataSet.length) + 5; })
      .attr("y", function (d) { return h - (d * 4) + 15;})
      .attr("font-family", "sans-serif")
      .attr("font-size", "11px")
      .attr("fill", "white")

  
  //Make the
  // There is a little trick with the y and the height attribute why is this
</script>
